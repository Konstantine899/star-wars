import React from "react";

const SearchPanel = () => {
  return (
    <input
      type="text"
      className="form-control search-input"
      placeholder="Поиск"
    />
  );
};

export default SearchPanel;
